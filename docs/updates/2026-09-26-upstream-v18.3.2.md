# Upstream v18.3.2 into neopi (2026-09-26)

- Source: `origin/neopi` at `da8246ac89` (Merge PR #93).
- Upstream: tag `v18.3.2` → commit `7853b4e499`. Lightweight tag (a commit, not a tag object): no signature to verify; recorded as unsigned.
- Upstream commits merged: 440.
- Recovery ref: `backup/neopi-pre-upstream-v18.3.2` → `da8246ac89`.
- Integration branch: `sync/upstream-v18.3.2` in `~/source/github/PsychedelicShayna/neopi-sync-v18.3.2`.
- Separate from open fork PRs #95 (live composer), #96 (chain context/lock), #97 (build scripts), which rebase after this lands.
- Conflicted paths: 73 (70 content, 3 modify/delete).

## Conflicts

| Path | Resolution | Fork behavior kept | Upstream change taken | Why |
| --- | --- | --- | --- | --- |
| `README.md` | whole file `ours` | Fork README (NeoPi lineage, fork PR list) | None; upstream edited its own marketing README (Stencil Labs credit, install text) | README.md is fork-owned since the rename; upstream's copy is linked from the lineage section |
| `nix/package.nix` | manual merge | `.npi-wrapped` / `$out/bin/npi` binary naming | CUDA provider soname install check | Upstream's new check runs the binary; reseated on the fork's `npi` name |
| `packages/agent/src/agent-loop.ts` | manual merge | Per-message steering interrupt mode (`SteeringQueueState.interruptMode`) and always watching an observable steering queue | `interruptMode: "wait"` semantics: interruptible waits are always cut short, only the soft signal is withheld; `shouldInterruptImmediately` removed | Adopted upstream's wait semantics and applied them per message: the soft signal now follows the message's own mode instead of the loop default |
| `packages/agent/src/types.ts` | manual merge (docs) | Per-message override wording on `hasSteeringMessages` | Upstream's `interruptMode` "wait" definition | Doc comments now describe the merged semantics from agent-loop.ts |
| `packages/ai/src/auth-broker/remote-store.ts` | theirs | None needed: the rebranded `APP_NAME` messages lived only in the removed methods | Removal of the client-side read-only mutation stubs; `AuthCredentialStore` type import | Upstream deleted the three throwing methods, so the fork's `APP_NAME` rebrand of their messages has nothing left to apply to |
| `packages/ai/src/auth-storage.ts` | theirs + brand re-seat | NeoPi wording in doc comments (`npi usage`, `npi auth`, NeoPi process, `neopi.internal`) | Split of AuthStorage into `packages/ai/src/auth/*` modules; this file becomes a 278-line facade | Fork changes were comment-only rebrands; re-applied to the moved text in `auth/types.ts`, `auth/pool.ts` |
| `packages/ai/src/provider-session-state.ts` | theirs + brand re-seat | NeoPi wording (1 line(s) re-applied) | `AuthStorage.limits.markReached` rename in docs | Fork delta was brand wording only |
| `packages/ai/src/providers/anthropic.ts` | theirs + brand re-seat | NeoPi wording (1 line(s) re-applied, 2 no longer present upstream) | Control-state types moved out of this file; tool-control planning doc rewritten | Fork delta was brand wording only |
| `packages/browser-relay/extension/manifest.json` | theirs + brand re-seat | NeoPi/npi wording (3 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/cli/config-cli.ts` | theirs + brand re-seat | NeoPi/npi wording (1 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/internal-urls/router.ts` | theirs + brand re-seat | NeoPi/npi wording (1 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/lsp/mux/protocol.ts` | theirs + brand re-seat | NeoPi/npi wording (1 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/tiny/title-client.ts` | theirs + brand re-seat | NeoPi/npi wording (3 line(s)) | All upstream edits in the file | Fork delta was brand wording only; missed lines re-applied by hand |
| `packages/coding-agent/src/tools/acp-bridge.ts` | theirs + brand re-seat | NeoPi/npi wording (3 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/tools/bash.ts` | theirs + brand re-seat | NeoPi/npi wording (1 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/test/debug/report-bundle-logs.test.ts` | theirs + brand re-seat | NeoPi/npi wording (2 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/metaharness/agent/omp_local.py` | theirs + brand re-seat | NeoPi/npi wording (17 line(s)) | All upstream edits in the file | Fork delta was brand wording only; missed lines re-applied by hand |
| `packages/tui/src/prompt/tips.txt` | theirs + brand re-seat | NeoPi/npi wording (6 line(s)) | All upstream edits in the file | Fork delta was brand wording only |
| `packages/coding-agent/src/tools/hub/index.ts` | deleted (theirs) | None: fork delta was `APP_NAME` wording in schema descriptions | Hub tool retired in favor of `wait` and protocol-based interfaces (f89a6db15e) | Upstream removed the file; brand wording is re-seated on the replacement surfaces in the post-merge sweep |
| `packages/coding-agent/src/prompts/tools/hub.md` | deleted (theirs) | None: fork delta was `APP_NAME` wording in schema descriptions | Hub tool retired in favor of `wait` and protocol-based interfaces (f89a6db15e) | Upstream removed the file; brand wording is re-seated on the replacement surfaces in the post-merge sweep |
| `packages/ai/src/utils/openrouter-headers.ts` | manual | Title stays PRODUCT_NAME (NeoPi) | Referer via new APP_URL constant | Upstream centralized gateway attribution in APP_URL; brand re-seat: APP_URL in utils/dirs.ts now points at the fork repo |
| `packages/catalog/scripts/proto-parser.ts` | ours | Brand-neutral header comment | Rest of file auto-merged | Only conflict was upstream renaming 'Oh My Pi' to 'omp' in a comment the fork had made brand-neutral |
| `packages/catalog/src/discovery/cursor-proto.ts` | ours | Brand-neutral header comment | Rest of file auto-merged | Only conflict was upstream renaming 'Oh My Pi' to 'omp' in a comment the fork had made brand-neutral |
| `packages/catalog/src/discovery/devin-proto.ts` | ours | Brand-neutral header comment | Rest of file auto-merged | Only conflict was upstream renaming 'Oh My Pi' to 'omp' in a comment the fork had made brand-neutral |
| `packages/coding-agent/src/blob-broker/uploaders-cloud-drives.ts` | ours | `Sent via ${PRODUCT_NAME}` | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/cli/command-help.ts` | ours | ACP help names PRODUCT_NAME | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/commands/acp.ts` | ours | Doc comment names NeoPi | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/commands/bench.ts` | manual | Examples keep ${APP_NAME} | New --detailed --par example | Upstream added an example; brand re-seat onto the fork's APP_NAME template form |
| `packages/coding-agent/src/commands/clip.ts` | manual | APP_NAME import for example text | Dropped DEFAULT_STREAM_URL import | Upstream stopped importing DEFAULT_STREAM_URL; fork still needs APP_NAME |
| `packages/coding-agent/src/cli/auth-broker-cli.ts` | theirs | APP_NAME usage strings elsewhere in the file (auto-merged); brand re-seat of the moved comments in cli/oauth-terminal.ts | Terminal login extracted to runTerminalOAuthLogin; unknown-provider error | Upstream moved the fork-branded login flow into oauth-terminal.ts; its comments re-branded there |
| `packages/coding-agent/src/config/settings.ts` | theirs + reapply | Runtime model loadout (getRuntimeModelOverrides / applyRuntimeOverridesAtomically) reseated on the registry: one snapshot, one #fireChangesSince instead of the removed modelRoles/runtime-config signals; legacy stt.modelName=xai → xai/grok-stt migration; NeoPi comment | Settings registry (6962d5cd2e): typed handles, onEffectiveChange listeners, removed string-path get/set and SettingSignal hooks | Upstream replaced the settings engine; the fork's additions were re-expressed against it. onRuntimeConfigChanged had no consumers and was dropped |
| `packages/coding-agent/src/config/settings-schema.ts` | deleted (upstream) + ported | chat.mode, chat.include → chat/settings.ts; chaining.auto, chaining.active → chains/settings.ts; chronicler.enabled → chronicler/settings.ts (all registered in all-settings.ts); advisor.immuneTurns removed from advisor/settings.ts (fork replaced it with severity-specific delivery, 2cd6808198); brand re-seat of descriptions in tools, tools/browser, lsp, collab, modes, session, stream, extensibility, task, config/model-settings | Settings split into per-domain registry modules | Upstream deleted the monolithic schema; each fork-only setting moved to a domain module following upstream's layout. The hub bash-interceptor message the fork branded no longer exists upstream |
| `packages/coding-agent/src/dap/session.ts` | ours | DAP clientName = PRODUCT_NAME | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/debug/index.ts` | ours | Notification title falls back to PRODUCT_NAME | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/extensibility/extensions/runner.ts` | union | Eval backend registry import | MAIN_AGENT_ID import | Both sides added independent imports |
| `packages/coding-agent/src/extensibility/legacy-pi-coding-agent-shim.ts` | theirs | — | Brand-neutral doc comment | Upstream rewrote the comment without a product name |
| `packages/coding-agent/src/main.ts` | manual | chat-mode branch owns tools/skills/rules/LSP; --no-lsp inside the non-chat branch; NeoPi comment; chat.mode/chat.include read via cfgChat* handles | applyProtocolDefaults via setting.protocolDefault; cfgSkillsIncludeSkills handle | Upstream replaced the host-defaulted path lists with a per-setting protocolDefault flag; the fork's restructured option block keeps its shape on the new handles |
| `packages/coding-agent/src/modes/acp/acp-agent.ts` | ours | ACP auth method and agentInfo name/title stay NeoPi | Rest auto-merged | Brand-only conflicts |
| `packages/coding-agent/src/modes/controllers/event-controller.ts` | ours | Notification titles fall back to PRODUCT_NAME | Rest auto-merged | Brand-only conflicts |
| `packages/coding-agent/src/stt/stt-controller.ts` | manual | Retained WAV-file cloud capture (recording kept on disk, xAI transport resolution); holdStart/holdEnd ownership re-expressed on start()/stop() so a space hold never stops a chord-started capture | start/stop/toggle with SttCallbacks + SttTarget; cfgSttLanguage handle | Upstream split toggle into explicit start/stop, which the fork's hold ownership now wraps |
| `packages/tui/src/prompt/custom-editor.ts` | theirs + reapply in space-hold.ts | Backspace latch during a space hold: SpaceHoldHandler.onLatch, latched state in SpaceHoldGesture (other keys type normally, Space/Backspace tap stops once released); both callers pass isBackspace | Space-hold state machine extracted to tui/space-hold.ts (SpaceHoldGesture + handler), shared with the /btw Input | Upstream moved the fork-adopted gesture out of CustomEditor; the fork-only latch now lives on the shared gesture so it also works in /btw |
| `packages/coding-agent/src/modes/controllers/input-controller.ts` | manual | isReservedKey import; REPL-eval-backend prefix detection (parseReplEvalInput) in the focused-view guard; chaining.* via cfgChaining* handles | InternalUrlRouter import; FOCUSED_VIEW_COMMANDS (/export, /usage) runnable from a focused subagent; space hold wired as editor.spaceHold.handler = ctx.dictationSpaceHold(editor) | Upstream's python-prefix helpers stay deleted (the fork moved them to repl-input.ts); fork hold gating (sttIdle, latch) moved into interactive-mode's dictationSpaceHold |
| `packages/coding-agent/src/modes/interactive-mode.ts` | manual | Ctrl+Space independent xAI controller (XaiSTTController) with cross-route guards; handleDictationToggle; sttIdle; chat-mode status label; space hold starts/stops only its own capture (holdStart/holdEnd), gated on sttIdle, Backspace latch status | MicCursor replaces the fork's per-mode HSV mic animation; dictationSpaceHold handler API; #readySTTController / #dictationCallbacks shape | Upstream's STT host refactor matched the fork's start/stop intent; the fork's two-route STT and hold ownership reseated on it. Dead animation fields dropped with upstream |
| `packages/coding-agent/src/modes/types.ts` | union | handleDictationToggle, sttIdle | dictationSpaceHold (replaces the fork's handleSTTHold) | The hold edges moved into the handler API |
| `packages/coding-agent/src/modes/rpc/host-uris.ts` | theirs + brand | Reserved-scheme error names PRODUCT_NAME; comment says harness-owned | Router-owned built-in scheme list replaces the local reserved set | Upstream moved reservation into InternalUrlRouter.isBuiltin |
| `packages/coding-agent/src/prompts/system/system-prompt.md` | manual | Role line names NeoPi coding harness | Internal URL list generated from InternalUrlRouter.describe (per-scheme prompt docs) | Upstream's generated URL docs are brand-neutral; the fork's hand-written list is obsolete |
| `packages/coding-agent/src/prompts/tools/task.md` | theirs + reapply | Direct model selector documentation for `agent` | Compressed Inputs section | Upstream shortened the prompt; the fork's selector contract (e4fadf1299) must stay visible to the model |
| `packages/coding-agent/src/sdk.ts` | manual | Chat mode: context files only when chat includes them, no forced session-managed builtins, memory/autolearn gated by memoryEnabled | SESSION_MANAGED_BUILTIN_TOOL_NAMES, cfgDisabledExtensions / cfgAutolearnEnabled handles | Fork chat-mode gates kept on upstream's handle-based reads |
| `packages/coding-agent/src/session/agent-session-events.ts` | union | hasFinalResponse (live voice relay) | yielded (turn finished; resumes only for queued input/background results) | Overlapping but distinct flags; kept both to avoid changing the live relay contract in the merge. Follow-up: evaluate reseating the relay on `yielded` |
| `packages/coding-agent/src/session/auth-broker-config.ts` | union | APP_NAME import | logger + registry imports | Independent import additions |
| `packages/coding-agent/src/session/eval-runner.ts` | ours | Kernel options come from the eval backend registry (fork eval backends), not the runner | cfgPython* handle reads | Upstream only converted reads the fork had already removed |
| `packages/coding-agent/src/session/session-advisors.ts` | manual | No immune-turn window (severity-specific delivery replaced it); advisor signature includes shared instructions, watchdog/context prompts and systemPrompt | #retuneAutoThinkingAdvisors on turn end; tier.advisor in the rebuild signature; cfgAdvisorMaxNotesPerUpdate handle | Fork removed the immune-turn window; upstream additions merged into the fork's signature |
| `packages/coding-agent/src/slash-commands/builtin-lifecycle.ts` | manual | APP_NAME import for the /restart description | Dropped unused capability reset import | Import-only conflict |
| `packages/coding-agent/src/tools/eval.ts` | manual | withTimeout import; language field accepts extension eval backends (string tokens, quoted fallback) | New language-field description format (`; ` join, no `runtime:` prefix); dropped unused threshold import | Fork eval-backend registry needs non-builtin tokens; upstream's wording kept |
| `packages/coding-agent/src/tools/ask.ts` | ours | Notification title PRODUCT_NAME | Rest auto-merged | Brand-only conflict |
| `packages/coding-agent/src/task/executor.ts` | theirs + reapply | Subagents never run their own Chronicler capture (chronicler.enabled forced false after overrides) | Live overlay of parent settings, root compaction thresholds, thinking-level snapshot | Upstream replaced the isolated snapshot with an overlay; the fork's single-capture-owner invariant re-added |
| `packages/coding-agent/src/session/agent-session.ts` | manual | Runtime model loadouts (resolveModelOverride, Settings.isolated candidate validation, RuntimeModelLoadout); Chronicler turn-end hook; hasFinalResponse for the live relay (emitted alongside upstream's yielded); chat-mode magic-keyword suppression and chatMode getter; PRODUCT_NAME power-assertion reason | Model-resolver/service-tier imports, registry combine/SettingsScope, CustomCommandContext + SkillDescriptionCatalog, Anthropic wrap-up steer, yielded/awaitingAsyncWork, cfgMagicKeyword handles, fallback-credit redemption | Every hunk was additive from both sides; string-path reads converted to handles |
| `packages/coding-agent/test/acp-initialize-conformance.test.ts` | ours | Assertion on upstream agentInfo name/title stays removed (fork ACP identity is neopi/NeoPi) | — | Brand assertion |
| `packages/coding-agent/test/advisor-toggle.test.ts` | manual | Advisor base-prompt override test | Async form of the role-change test | Adjacent additions |
| `packages/coding-agent/test/agent-session-auto-compaction-queue.test.ts` | manual | Parameterized delivery variants of the parked-command resume test | cfgCompaction* handles | Fork generalized the test; upstream converted settings access |
| `packages/coding-agent/test/blob-uploaders-cloud-drives.test.ts` | ours | Expects 'Sent via NeoPi' | — | Brand |
| `packages/coding-agent/test/modes/components/settings-layout.test.ts` | manual | No advisor.immuneTurns | Untyped path list | Fork removed the setting |
| `packages/coding-agent/test/power-assertion-options.test.ts` | ours | Brand-literal assertion stays removed | — | Pinned upstream product wording |
| `packages/coding-agent/test/rpc-host-uris.test.ts` | theirs + brand | Reserved-scheme message names PRODUCT_NAME | Every built-in scheme rejected, clear() preserves built-ins | Upstream broadened the test |
| `packages/coding-agent/test/stt-cloud.test.ts` | manual | Temp agent dir for retained WAV files | cfgSttSubmitTrigger handle | Additive |
| `packages/coding-agent/test/settings-manager.test.ts` | manual | Chronicler persistence + subagent exclusion test, on handles | Upstream 'effective values' suite replaces the removed group-cache test | Fork test ported to the registry API |
| `packages/tui/src/desktop-notify.ts` | ours | Desktop notification app name = PRODUCT_NAME | Rest auto-merged | Brand-only conflicts |
| `packages/tui/src/terminal-capabilities.ts` | ours | cmux/OSC99 app names = PRODUCT_NAME | Rest auto-merged | Brand-only conflicts |
| `scripts/bazel-natives.ts` | manual | OMP_NATIVE_X64_VARIANT override | musl host detection | Both fields apply to the host descriptor |
| `packages/tui/test/desktop-notify.test.ts` | ours | Assertions built from PRODUCT_NAME | — | Brand-only conflicts |
| `packages/tui/test/notifications.test.ts` | ours | Assertions built from PRODUCT_NAME | — | Brand-only conflicts |
| `bun.lock` | upstream lockfile, four packages held at prior versions | @huggingface/hub 2.17.2, @huggingface/tasks 0.21.46, oxlint 1.83.0 (+ platform bindings), yargs 18.1.0 | Workspace version bumps and every other lockfile entry | Upstream's bumps (hub 2.17.5 published 09-22, tasks 0.21.48 09-22, oxlint 1.85.0 09-21, yargs 18.2.0 09-20) are inside the 7-day cooldown for npm packages; declared ranges accept the prior versions. No new dependencies were introduced (package.json dependency blocks unchanged apart from workspace versions) |
| `packages/catalog/src/models.json` | theirs | — | Upstream-generated catalog | The fork's delta was a regeneration snapshot at another date (display names, tps, kimi slug), not fork policy |
| `packages/catalog/src/compat/rules.json` | regenerated | Fork KDL rules (xai.kdl, runtime/behavior.kdl) compiled in | Upstream KDL tree | Generated from the merged rule tree with `bun run gen:compat` |

Brand re-seat: `packages/utils/src/dirs.ts` `APP_URL` → fork repository; upstream-added user text routed through `APP_NAME`/`USER_AGENT` in `ida/client.ts`, `cli/auth-broker-cli.ts`, `cli/git-tui/avatar.ts`, `cli/grievances-cli.ts`, `commands/toks.ts`, `cli/oauth-terminal.ts`, and the per-domain setting descriptions.

## Post-merge fixes (separate signed commits)

- Fork-only settings reads moved onto registry handles (Chronicler, /chaining, advisor prompt preview, xAI Ctrl+Space submit trigger, task overrides); Chronicler listens synchronously via `onEffectiveChange`.
- Space-hold Backspace latch is unconditional on `SpaceHoldGesture` (`onLatch` is only a notification).
- `agent-loop.ts`: mid-batch steering interrupts only when its effective mode is `immediate`. Upstream 6e2eb2ef88 made queued steering always abort interruptible waits even under `wait`; that cut held tools short on advisor concerns (delivered with per-message `wait`). Default `interruptMode` is `immediate`, so ordinary steering still cuts the upstream `wait` tool short. The upstream test pinning the opposite contract was removed.
- Catalog: `xai-oauth/grok-stt` spliced back into upstream's generated `models.json` (full `gen:models` drifted 140k lines and broke an unrelated QwenCloud bundle test).
- Tests ported to handles, `auth.keys.setRuntime`, `auth.health.model`, argument-free `holdEnd`; advisor severity tests wait for the routed note because upstream ends advise-only advisor turns without another model call.

## Verification

- `bun install --frozen-lockfile`: ok.
- `CARGO_BUILD_JOBS=6 bun run build:native`: ok (local profile, 6m18s).
- `bun run --filter './packages/*' check:types`: all packages pass. `bun run check:tools` (oxlint + oxfmt): pass.
- `bun --cwd=packages/agent test`: 659 pass, 0 fail.
- `bun --cwd=packages/catalog test`: 979 pass, 0 fail.
- `bun --cwd=packages/ai test`: 5052 pass, 6 fail → after the attribution test fix, the remaining 5 are Bedrock tests that time out only in the full run and pass in isolation (`bedrock-caller-headers`, reasoning-off caps).
- `bun --cwd=packages/tui test`: 2886 pass, 2 fail (`attachment-chips` Kitty thumbnails; identical failures on the pre-merge base).
- `bun run test:scripts`: 42 pass, 0 fail.
- `bun --cwd=packages/coding-agent test` (with `GIT_CONFIG_GLOBAL` pointing at a signing-free config so fixture repos can commit): 15786 pass, 34 fail. 32 reproduce with the same counts on the pre-merge base (`input-controller-loop` 9, `input-controller-orphan-submit` 5, `input-controller-python-prefix` 2, `settings-layout` 2, `skills` 3, `changelog` 2, and one each in `agent-session-yield-empty-stop-suppression`, `session-selector-delete`, `session-loader-stream`, `settings-manager`, `stream/streamer`, `browser-attach`, `puppeteer-stealth-patch`, `update-cli`, `changelog-static-import`). Two fail only in the full run and pass in isolation on both trees (`system-prompt-template` SYSTEM.md precedence, `issue-985-subagent-auth-fallback` disabled provider): order-dependent, not proven pre-existing.
- Focused Ctrl+Space / STT: `xai-stt-controller`, `stt-xai-batch`, `stt-hold-gesture`, `stt-cloud`, `input-controller-keybindings` pass.

## Binary

- `packages/coding-agent/dist/npi --version` → `npi/18.3.2`; `--smoke-test` → `smoke-test: ok`.
- Staged install at `/tmp/npi-stage/bin/npi` with `PI_CODING_AGENT_DIR=/tmp/npi-stage/agent`: `install-neopi-extensions.ts` linked neopi-live-persona, neopi-loadout, neopi-persona, neopi-repl to this worktree; `--version`, `--smoke-test`, `--help` exit 0; `-p "Reply with exactly: OK"` → `OK` (exit 0, credentials copied into the staging dir and removed after).
- Ctrl+Space in the staged TUI: first tap showed the recording mic cursor, second tap finished with "No speech detected." (silent room). Live `~/.local/bin/npi` and profile untouched.
